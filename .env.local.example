# Sanity CMS — fill these in after creating a project (npx sanity@latest init)
# then copy this file to `.env.local`. Until set, the /menu page uses mock data.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01
